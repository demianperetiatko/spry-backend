version: '3.8'
services:
  db:
    image: postgres:latest
    volumes:
      - postgres_data:/var/lib/postgresql/data
    environment:
      POSTGRES_DB: spry
      POSTGRES_USER: spry
      POSTGRES_PASSWORD: spry_pwd
    ports:
      - "5432:5432"

volumes:
  postgres_data:
